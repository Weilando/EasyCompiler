// 9 type errors
void main() {
  int a = true; // wrong init
  int b = 2;
  int a;  // already defined
  boolean c = false;
  boolean c = true; // already defined
  boolean d = 1; // wrong init
  e = 4;  // not declared
  print(f); // not declared
  println(42 % true); // incompatible types in expression
  while(3) { // incompatible type in expression
    a = b && c; // incompatible types
  }
}
