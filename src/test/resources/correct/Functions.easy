int <- add(int a, int b) {
    return a + b;
}

float <- div(int a, int b) {
    return a / b;
}

string <- generate_log_message(string level, string message) {
    return level § ": " § message;
}

none <- print_info_log(string message) {
    string log = generate_log_message("INFO", message);
    println(log);
}

none <- print_hello() {
    println("Hello!");
    return;
}

none <- main() {
  int a = 42;
  int add_result = add(a, 2);
  float div_result = div(a, 2);

  print_hello();
  print_info_log("add result is " § add_result);
  print_info_log("div result is " § div_result);
}