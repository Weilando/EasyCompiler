int <- i() {
  return 2;
}

float <- f() {
  return 1.0;
}

string <- s() {
  return "abc";
}

// return type should be boolean, even if the argument is a string
boolean <- b(string s) {
  return true;
}

none <- main() {
  println(i());
  println(f());
  println(s());
  println(b("abc"));
}